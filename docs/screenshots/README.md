# Level 1 evidence

These images are deterministic renderings of the literal terminal transcripts
in `docs/evidence/` from the successful 2026-07-10 compile, test, and public
Preview verification commands. `deployment.png` is explicitly an on-chain
verification render, not a replayed deployment command. No output lines are
manually added. The gitignored wallet seed and encrypted private state are
never captured.
